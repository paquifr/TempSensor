#pragma once
#include "pch.h"

class wireProtocol
{
	public:
		// wireProtocol(GPIOConnection& connection);
		wireProtocol();

		/// <summary>
		/// Launch the data acquisition process from the DHT11 chip.
		/// </summary>
		void launch();

 	private:

};